# MyPet asset pipeline - install into the E-drive Python 3.11 embeddable
# ranges kept conservative so every wheel has a py3.11 win_amd64 build
numpy>=1.26.0,<2.3
onnxruntime>=1.17.0,<1.23
pillow>=10.2.0,<12
rembg>=2.0.57,<2.1
